extends ../_layouts/default

block vars
  - base_url = '../'

block nav
  +nav('nav', global.nav)
  +nav('nav--light', global.navejemplos)

block content
  .wrapper
    section#content.span5.span-768-6
      h3 Galeria a 6 columnas
      +gallery(6, gallery)
    .span7.span-768-6
      h3 Galeria a 4 columnas
      +gallery(4, gallery)